
MEMCMP(3)                BSD Library Functions Manual                MEMCMP(3)

NNAAMMEE
     mmeemmccmmpp -- compare byte string

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttrriinngg..hh>>

     _i_n_t
     mmeemmccmmpp(_c_o_n_s_t _v_o_i_d _*_s_1, _c_o_n_s_t _v_o_i_d _*_s_2, _s_i_z_e___t _n);

DDEESSCCRRIIPPTTIIOONN
     The mmeemmccmmpp() function compares byte string _s_1 against byte string _s_2.
     Both strings are assumed to be _n bytes long.

RREETTUURRNN VVAALLUUEESS
     The mmeemmccmmpp() function returns zero if the two strings are identical, oth-
     erwise returns the difference between the first two differing bytes
     (treated as _u_n_s_i_g_n_e_d _c_h_a_r values, so that `\200' is greater than `\0',
     for example).  Zero-length strings are always identical.  This behavior
     is not required by C and portable code should only depend on the sign of
     the returned value.

SSEEEE AALLSSOO
     bcmp(3), strcasecmp(3), strcmp(3), strcoll(3), strxfrm(3), wmemcmp(3)

SSTTAANNDDAARRDDSS
     The mmeemmccmmpp() function conforms to ISO/IEC 9899:1990 (``ISO C90'').

BSD                              June 4, 1993                              BSD
